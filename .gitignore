# 시스템 파일 (Git에 올릴 필요 없는 것들)
.DS_Store
Thumbs.db
desktop.ini

# 문서 파일
*.docx
*.pptx
*.xlsx

# 보안 - 환경변수 및 API 키 (절대 GitHub에 올리면 안 됨)
.env
.env.local
.env.development
.env.production
.env.*.local

# 보안 - 키/인증서 파일
*.key
*.pem
*.p12
*.pfx
secrets.js
secrets.json
config.secret.js

# 보안 - API 키를 담을 수 있는 설정 파일
api-keys.js
api-keys.json
private-config.js
